query allQuestions {
    allQuestions {
        title
        titleSlug
        frontendQuestionId: questionFrontendId
        questionId
        status
        level: difficulty
        isPaidOnly
        category: categoryTitle
    }
}